<svg x='{{area.position.x}}' y='{{area.position.y}}' width='{{area.size.width}}' height='{{area.size.height}}'>
  {{{area.contents}}}
  {{#each area.contains as |child|}}
    <a xlink:href='/areas/{{child.id}}'>
      {{area-svg area=child.area}}
    </a>
  {{/each}}
</svg>

{{yield}}
